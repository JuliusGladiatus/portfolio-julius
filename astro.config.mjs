// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://juliusgladiatus.github.io',
  base: '/portfolio-julius',
  output: 'static',
});
